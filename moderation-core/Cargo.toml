[package]
name = "moderation-core"
version = "0.1.0"
edition = "2021"

[dependencies]
plonky2 = "=0.2.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

[dev-dependencies]
# (tests use the std lib only)
